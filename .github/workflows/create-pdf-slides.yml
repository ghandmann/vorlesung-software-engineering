name: Create PDF slides
on:
  push:
    tags:
      - '*'

jobs:
  create-pdf-slides:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Convert markdown to PDF slides
        run: docker run --rm -e MARP_USER="$(id -u):$(id -g)" -v "${{ github.workspace }}/presentation:/home/marp/app" marpteam/marp-cli:main --allow-local-files --theme-set vorlesung.css --pdf -I . -o slides/
      - name: Release slides
        run: gh release create -d --title "Vorlesungsfolien $GITHUB_REF_NAME" $GITHUB_REF_NAME presentation/slides/*.pdf
        env:
          GH_TOKEN: ${{ github.token }}